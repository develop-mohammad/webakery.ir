<?php
/**
 * تنظیمات فرم پرداخت اینماد — enamad-order
 * این فایل را ویرایش کنید و مقادیر واقعی را جای‌گزین کنید.
 */

// ─── درگاه پرداخت زیبال ──────────────────────────────────────────
// همان مرچنت‌کد درگاه زیبال سایت وب‌آکری (مشابه license-server/config.php).
// برای تست سندباکس زیبال موقتاً مقدار را به 'zibal' تغییر دهید.
define( 'EO_ZIBAL_MERCHANT', 'fc6fd44c-0e7d-4693-ae42-f7ccc29116d9' );

// ─── هزینه نهایی خدمات دریافت اینماد (به ریال) ───────────────────
// ۵,۰۰۰,۰۰۰ تومان = ۵۰,۰۰۰,۰۰۰ ریال
define( 'EO_PRICE_RIAL', 50000000 );

// ─── نام نمایشی خدمت (در فاکتور و صفحه پرداخت) ──────────────────
define( 'EO_SERVICE_TITLE', 'خدمات دریافت نماد اعتماد الکترونیکی (اینماد)' );

// ─── آدرس پایه سایت ──────────────────────────────────────────────
define( 'EO_BASE_URL', 'https://webakery.ir' );

// آدرس عمومی همین فرم (کالبک زیبال و لینک اشتراک در تلگرام/اینستاگرام)
define( 'EO_PUBLIC_URL', 'https://webakery.ir/enamad-order/' );

// ─── ورود پنل مدیریت (فاکتور جامع داخلی) ─────────────────────────
// بعد از اولین ورود حتماً این‌ها را روی سرور واقعی عوض کنید.
define( 'EO_ADMIN_USER', 'admin' );
define( 'EO_ADMIN_PASS', 'change-this-password' );

// ─── اعلان سفارش پرداخت‌شده به تیم (اختیاری) ─────────────────────
// ایمیلی که فاکتور جامع داخلی برایش ارسال می‌شود. خالی = ارسال نشود.
define( 'EO_NOTIFY_EMAIL', '' );

// ربات تلگرام برای اعلان سفارش جدید (اختیاری). خالی = ارسال نشود.
define( 'EO_TG_BOT_TOKEN', '' );
define( 'EO_TG_CHAT_ID', '' );

// ─── اطلاعات تماس پشتیبانی (نمایش در فرم و فاکتور مشتری) ─────────
define( 'EO_SUPPORT_EMAIL', 'info@webakery.ir' );
define( 'EO_SUPPORT_TELEGRAM', '@webakery_support' );
