appId: ir.webakery.daftarchi
productName: Daftarchi
copyright: Copyright © webakery.ir
directories:
  output: release
  buildResources: resources
files:
  - dist/**/*
  - dist-electron/**/*
  - package.json
  - "!**/*.linux.node"
asar: true
asarUnpack:
  - "**/*.node"
  - "**/better-sqlite3/**"
extraMetadata:
  main: dist-electron/main.js
extraFiles:
  - from: resources/INSTALL-WINDOWS.txt
    to: INSTALL-WINDOWS.txt
npmRebuild: false
win:
  executableName: Daftarchi
  icon: resources/icon.png
  signAndEditExecutable: false
  artifactName: "${productName}-${version}-win-${arch}.${ext}"
  target:
    - target: zip
      arch:
        - x64
    - target: nsis
      arch:
        - x64
nsis:
  artifactName: "${productName}-Setup-${version}.${ext}"
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  allowElevation: true
  createDesktopShortcut: always
  createStartMenuShortcut: true
  shortcutName: Daftarchi
  uninstallDisplayName: Daftarchi
  installerLanguages:
    - en_US
  language: "1033"
  deleteAppDataOnUninstall: false
portable:
  artifactName: "${productName}-Portable-${version}.${ext}"
linux:
  target:
    - AppImage
  category: Office
  icon: resources/icon.png
  artifactName: "${productName}-${version}.${ext}"
